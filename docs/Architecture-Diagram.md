# Architecture Diagram (Text Overview)

This document replaces the previous diagram with a simplified text description.

## Layer Overview (Top to Bottom)

- Evolution — Long-term refinement and meta-alignment
- Integrity — Safety, coherence, and constraint enforcement
- Execution — Deterministic workflow generation
- HUD — Salience, clarity, and user-facing context
- State — Memory, continuity, and stable identity
- Interpreter — Intent alignment and identity stabilization
- Doctrine — Core values, constraints, and identity foundation

This textual diagram preserves clarity without ASCII formatting issues.
